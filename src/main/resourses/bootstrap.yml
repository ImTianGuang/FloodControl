spring:
  application:
    name: demo-service

server:
  port: 8081

spring:
  datasource:
  driverClassName: com.mysql.jdbc.Driver
  url: jdbc:mysql://localhost:3306/flood?useUnicode=true&characterEncoding=utf-8
  username: root
  pasword: root

mybatis:
  mapper-locations: classpath:/mapper/*.xml


